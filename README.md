# ai site-builder scaffold

This repository was initialized by GitHub Copilot scaffolding for an AI-driven static site builder.

Default branch contains a minimal initial commit. A feature branch `site-builder-scaffold` contains the full scaffold (generator, image optimizer, CLI, and GitHub Actions workflow).

See the `site-builder-scaffold` branch for full files.
